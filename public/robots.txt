# Resolve · robots.txt
# Fully open to all crawlers, including AI / answer-engine crawlers
# (GPTBot, ClaudeBot, CCBot, Google-Extended, PerplexityBot, etc.) so the
# site is discoverable in both classic search and AI-generated answers.
# Only the SPA 404 redirect artifacts are excluded so the transient
# redirect URL is not indexed.

User-agent: *
Allow: /
Disallow: /404.html
Disallow: /*?/*

Sitemap: https://www.resolverealestate.ca/sitemap.xml
